const Link = (props: React.AnchorHTMLAttributes<HTMLAnchorElement>) => <a {...props}/>;
import {entries} from '@/content';
export function LatestArticles(){const items=entries('articles').slice(0,3);const [first,...rest]=items;return first?<div className="article-grid"><Link className="feature" href={`/articles/${first.slug}`}><div className="type-art" dir="ltr"><small>EVERY IDEA HAS A STORY.</small><strong style={{fontSize:'clamp(45px,6vw,100px)',letterSpacing:'-2px',overflowWrap:'anywhere'}}>{first.tags.find(t=>/^[a-z0-9+#.]+$/i.test(t))||'READ'} ↗</strong><span>01 ───── 02 ───── 03</span></div><span className="meta">{first.category} · {first.minutes||3} دقائق {first.sample?'· محتوى تجريبي':''}</span><h3>{first.title}</h3><p>{first.description}</p></Link><div className="side-articles">{rest.map(item=><Link href={`/articles/${item.slug}`} key={item.slug}><span className="meta">{item.category} · {item.minutes||3} دقائق</span><h3>{item.title}</h3><p>{item.description}</p><span className="arrow">↖</span></Link>)}</div></div>:<p className="empty">أول حكاية رح تكون هون.</p>}
export function LatestPrompts(){return <>{entries('prompts').slice(0,2).map((item,i)=><Link className="prompt-row" href={`/prompts/${item.slug}`} key={item.slug}><span className="index">0{i+1}</span><div><span className="meta">{item.category}</span><h3>{item.title}</h3></div><span className="pill">افتح البرومبت ↖</span></Link>)}</>}
